PORT=8080

# Use Postgres if set, otherwise in-memory store is used
# DATABASE_URL=postgres://user:pass@localhost:5432/gpsnav?sslmode=disable

# Optional Redis for cross-process event broker
# REDIS_URL=redis://localhost:6379/0

# Auth modes: dev | hmac | jwks
AUTH_MODE=dev
# AUTH_HMAC_SECRET=dev-secret
# AUTH_JWKS_URL=https://example.com/.well-known/jwks.json

# Webhooks
WEBHOOK_MAX_ATTEMPTS=10

